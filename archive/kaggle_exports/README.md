# Archived Kaggle Exports

These two large Python files were originally exported from the Kaggle notebook:
https://www.kaggle.com/code/prnavjoshi/warehouse-anomaly-detection-stress-testing

They are **not used** by the active pipeline in `src/` or `run_src.py`.
They are kept here purely for reference / historical purposes.

**Do not run these files directly.** Use `python run_src.py` (or the version in notebooks/) instead for the maintained pipeline.